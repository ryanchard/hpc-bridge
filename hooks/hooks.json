{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash|mcp__hpc-bridge__run_shell|mcp__hpc-bridge__login_shell",
        "hooks": [
          { "type": "command", "command": "${CLAUDE_PLUGIN_ROOT}/hooks/credential-guard.sh", "timeout": 10 }
        ]
      }
    ]
  }
}
