{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "hpcb-mep-strict user_endpoint_config",
  "description": "STRICT contract (Anvil's shape): only the scheduler knobs below; anything else -- compute, interface, worker_init, provider_type -- is REJECTED by the manager, so a client must drop what it cannot send and rely on the template's defaults.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "partition": {"type": "string", "enum": ["debug", "compute", "gpu"]},
    "account": {"type": "string"},
    "walltime": {"type": "string", "pattern": "^[0-9]{1,3}:[0-5][0-9]:[0-5][0-9]$"},
    "nodes_per_block": {"type": "integer", "minimum": 1, "maximum": 3},
    "max_workers_per_node": {"type": "integer", "minimum": 1, "maximum": 4},
    "init_blocks": {"type": "integer", "minimum": 0, "maximum": 2},
    "max_blocks": {"type": "integer", "minimum": 0, "maximum": 2}
  }
}
