# hpc-bridge fake cluster — slurm.conf for the `default` profile (mounted from profiles/default/ at run time).
# Deliberately minimal and container-friendly: no cgroups, no task affinity, munge auth,
# accounting through slurmdbd so `sacct` works (the harness postchecks read squeue/sacct).
ClusterName=fake
SlurmctldHost=slurmctld

AuthType=auth/munge
CredType=cred/munge
MpiDefault=none
ProctrackType=proctrack/linuxproc
TaskPlugin=task/none
SwitchType=switch/none
ReturnToService=2
MailProg=/bin/true

SlurmctldPidFile=/var/run/slurmctld.pid
SlurmdPidFile=/var/run/slurmd.pid
SlurmctldPort=6817
SlurmdPort=6818
SlurmdSpoolDir=/var/spool/slurmd
StateSaveLocation=/var/spool/slurmctld
SlurmUser=slurm

# Containers all see the host VM's CPUs/RAM; config_overrides makes slurmd trust the NodeName
# line instead of refusing to register on a hardware mismatch.
SlurmdParameters=config_overrides

InactiveLimit=0
KillWait=30
MinJobAge=300
SlurmctldTimeout=120
SlurmdTimeout=300
Waittime=0

SchedulerType=sched/backfill
SelectType=select/cons_tres
SelectTypeParameters=CR_Core

# Accounting via slurmdbd (+ MariaDB) — what makes `sacct`/`sacctmgr` real. No enforcement, so a
# job needs no --account (like globus1's AccountingStorageEnforce=none / account_required: false).
AccountingStorageType=accounting_storage/slurmdbd
AccountingStorageHost=slurmdbd
AccountingStoragePort=6819
JobAcctGatherType=jobacct_gather/none

SlurmctldDebug=info
SlurmdDebug=info

# Two compute nodes; a single default partition named `main` (what the harness expects), 2-day
# walltime like globus1.
NodeName=c[1-2] CPUs=4 RealMemory=4000 State=UNKNOWN
PartitionName=main Nodes=c[1-2] Default=YES MaxTime=2-00:00:00 State=UP
