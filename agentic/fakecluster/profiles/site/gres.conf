# Two fake GPUs on c3, backed by dummy /dev/nvidia0-1 device files that setup.d/slurmd.sh creates at start (Slurm's
# gres/gpu plugin drops file-less GPUs; Flags=CountOnly did not help on 23.11). Enough for `--gpus` scheduling.
AutoDetect=off
NodeName=c3 Name=gpu File=/dev/nvidia[0-1]
