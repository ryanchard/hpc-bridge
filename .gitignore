# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/

# Obsidian machine-local UI state (keep the vault, drop the cursor/layout)
docs/hpc-bridge-vault/.obsidian/workspace.json
docs/hpc-bridge-vault/.obsidian/workspace-mobile.json

# Secrets — NEVER commit (plaintext OAuth token / cred paths for the agentic harness)
agentic/.env
*.env

# Per-run agentic provenance bundles (transcripts, message streams) — artifacts, not source
agentic/runs/
